export enum PaymentMethodsEnum {
  PIX = 'pix',
  STRIPE = 'stripe',
  STONE_CARD = 'stone_card',
  PAYPAL = 'paypal',
}

export enum ThemeShowTypeEnum {
  BLUE = 'blue',
  PINK = 'pink',
  YELLOW = 'yellow',
  DEFAULT = 'default',
}

export enum Sex {
  MALE = 'male',
  FEMALE = 'female',
}
